Sort where order does not matter
The Arrays.toString(array) method is then used to print the reversed array as a string for display purposes. If you want to print the array without converting it to a string, you can loop through the array and print each element individually.
